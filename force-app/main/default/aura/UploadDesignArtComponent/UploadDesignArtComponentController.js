({
    getDesignBoundAttributes : function(component, event, helper) 
    {
        helper.getDesignBoundAttributes(component, event);
    },
    handleCloseModal: function(component, event, helper) 
    {
        helper.handleCloseModal(component, event);
    },
    handleCancel : function(component, event, helper)
    {
        helper.handleCancel(component, event);
    },
    handlePassAllErrorOnMainComponent : function(component, event, helper)
    {
        helper.handlePassAllErrorOnMainComponent(component, event);
    }
})